<div class="container-fluid">
<h1>All Posts</h1>

	<% @posts.each do |post| %>
		<h2>
			<%= link_to post.title, post %>
		</h2>
		<p>
			<%= post.created_at.strftime("%B, %d, %Y") %>
		</p>
	<% end %>		

<br>
<%= will_paginate @posts %>
<br>
<br>
</div>